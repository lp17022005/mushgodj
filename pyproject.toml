[tool.poetry]
name = "mushgo"
version = "0.1.0"
description = ""
authors = ["Letícia Patricio"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
Django = "^4.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
